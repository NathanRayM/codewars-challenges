/*
Description:
Write a function which converts the input string to uppercase.
*/

function makeUpperCase(str) {
  let greeting = str.toUpperCase();
  return greeting;
}
console.log(makeUpperCase("hello world!")); //Output: HELLO WORLD!
